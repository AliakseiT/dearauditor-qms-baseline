name: Auto Training Assignment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecation_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "Auto per-SOP training issue creation is deprecated."
          echo "Use release_training_diff.yml for consolidated per-user training obligations."
